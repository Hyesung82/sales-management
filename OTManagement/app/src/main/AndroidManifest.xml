<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.otmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OTManagement">
        <activity android:name=".UserInfo"></activity>
        <activity android:name=".WarehouseDetail" />
        <activity android:name=".CountriesByRegion" />
        <activity android:name=".CustomerDetail" />
        <activity android:name=".CustomerInfo" />
        <activity android:name=".ItemByName" />
        <activity android:name=".ItemByCategory" />
        <activity android:name=".ProductInfo" />
        <activity android:name=".Login" />
        <activity android:name=".Splash" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>